import React from 'react';
import './Spinner.scss';

const Spinner = () => {

    return (
        <div className="loader">
            <img src="/img/loader-preview.svg" alt="loading" />
        </div>
    )
}

export default Spinner;
